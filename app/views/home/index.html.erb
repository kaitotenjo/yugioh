<div class="container">
    <div class="row">
        <% @cards.each do |card| %>
        <%= render 'home/card', card:card %>
        <% end %>
        <%= paginate @cards , theme: 'twitter-bootstrap-3'%>
    </div>
</div>
